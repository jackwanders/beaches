node_modules
dist
*.local
.DS_Store

# Playwright MCP scratch output (snapshots, screenshots, console logs)
.playwright-mcp

# public/assets is committed on purpose — the mirrored heroes, logos and menu
# PDFs are what makes the app work offline.
